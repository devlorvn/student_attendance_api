export enum ErrorCode {
  WRONG_VALIDATE = -1,
}

export enum StudentErrorCode {
  NOT_FOUND = 1,
}

export enum AdminErrorCode {
  EXIST_EMAIL = 101,
  LOGIN_FAILED = 102,
  INVALID_POSITION = 103,
}
